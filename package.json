{
  "name": "claude-agents-course",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hands-on course repo — building with tools, skills, agents & subagents on the Claude SDK (TypeScript).",
  "scripts": {
    "week1": "tsx week1/hello.ts",
    "week1:env": "tsx --env-file=.env week1/hello.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.234",
    "@anthropic-ai/sdk": "^0.117.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
